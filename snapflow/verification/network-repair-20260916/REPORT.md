# Windows 网络修复验证 · 2026-09-16

- 后端及 HTTP 离线回归：116 项通过（含 11 项连接/状态检查和 3 项更新器检查）。更新器扩充为完整 20 个运行文件后，其 3 项备份/回滚检查再次通过。
- Chrome 离线交互：12 组通过，涵盖目录授权、批量处理、编辑、暂停/恢复、窄屏及初始连接错误。最初两次浏览器导航超时；隔离 Chrome、禁用代理并延长导航等待后完成。
- 转接 TLS 冒烟：开发机本地 TCP 转发，保留原网关 SNI、证书及域名校验，无密钥 GET `/v1/models` 返回 HTTP 401。没有测试 Windows SSH 身份。
- 开发机使用已有配置 GET 模型列表返回 HTTP 200，且列出 `gemini-3.8-flash`；没有发送图片或调用模型。网关在开发机解析为内网地址，这支持优先尝试已有 SSH 路径，但不能代替 Windows 实机验证。
- 一键入口只更新指定安装，先备份并逐文件核验 SHA-256 和大小；保留凭据、数据库、相册授权。连接检测通过后至多重试一张此前失败的截图，普通模型请求无自动重试。
- SSH 备用路径仅为指定网关启用，复用已有可信 SSH 登录，不安装服务、不修改主机信任、系统代理或防火墙；通过开发机时依赖开发机和 Tailscale 在线。
- Windows 上执行修复、实际截图成功、持续监测和飞书真实账号仍待验收。文件生成/传输不等于运行成功。

源码依据：[Python HTTPSConnection](https://docs.python.org/3.12/library/http.client.html#http.client.HTTPSConnection)、[OpenSSH 本地端口转发](https://man.openbsd.org/ssh.1)。
