# AgentM 工作约定

## 保存进展

- 用户已授权将本项目推送到 `git@github.com:Ethan-Li-k/AgentM.git`。
- 每次完成一个有明确进展、可以复核的修改后，检查差异，执行适当验证，创建描述清楚的 Git 提交，并推送到当前工作分支。无须每次重复请求推送许可。
- 默认分支为 `main`。推送前核对远端状态，保留既有历史；不得强制推送或重写他人提交。
- 不要把尚未实现或尚未在 Windows 验证的功能写成已经完成。把当前问题和下一步更新到 `PROJECT_STATUS.md`。
- 每次取得进展并保存版本时，同步核对 README 的流程图、完成状态、待办、验证结果及版本入口；状态发生变化就更新，并与 PROJECT_STATUS.md 保持一致。
- 密钥、令牌、`.env`、用户截图、数据库、私有配置、临时目录和历史恢复归档不能提交。提交前检查暂存文件清单及实际内容。
- 发布包不得包含真实凭据或用户数据；更新代码不能覆盖用户配置与数据。

## 本机与任务边界

- 本机项目读取、写入和构建仅在本项目目录内；中间产物统一放到 `tmp/<任务名>/`。
- 本机用于开发和协调，不执行训练。训练或模型检验须遵守用户提供的云主机边界及具体授权；普通离线单元测试使用替身模型。
- 预计超过 60 秒的命令用 tmux 持久运行并将输出通过 tee 写入任务日志。
- 删除数据、评测记录或历史版本前，先归档并逐文件验证大小和 SHA-256；保护密钥、用户数据以及归档目录。
- 以本次会话中的用户授权、拓扑要求和任务约束为准，不在仓库内保存私钥或连接凭据。
