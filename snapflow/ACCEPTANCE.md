# SnapFlow 2026-09-13 验收

- 网上收集 30 张公开图片，包含聊天通知、PPT/作业、活动海报、搞笑图、天气图与应用截图；原图、规范化输入、来源 URL 和 SHA-256 随包交付。
- 最终提示和 Gemini 3.8 Flash 网关路由对 30 张均返回有效结果，提取 35 条事项；分类和关键规则检查通过。完整响应中位数 8.02 秒。该样例集已用于改进提示，不是独立盲测或逐字 OCR 准确率证明。
- 浏览器真实一次选入 30 张原图，回放对应云端响应后统一保存 29 条行动；6 条缺少起止时间的日程留待核对，没有自动编造时间。
- 任务缺截止时间可以直接保存，模型补充问题不再强制逐条勾选。
- 直接打开系统相册选择器；桌面文件夹只读授权、缩略图选择、取消与不支持时降级；GIF/BMP 可解码时转静态图片。系统权限弹窗与手机真机未实测。
- 飞书登录、state/PKCE、浏览器 Cookie 绑定、防重放、刷新、读取可写日历、选择目标、创建日程及重复同步保护已实现。真实账号尚未授权，未真实创建飞书日程。
- 54 项后端 / HTTP 测试、33 组 Chrome 交互检查通过。模型请求仅在用户指定云主机执行，本机界面使用离线回放。
- 最终默认 gemini-3.8-flash；已存在的显式 .env 设置继续优先。120 秒等待、两路并发、不自动重试。共 90 次云端调用，费用未取得人民币账单。
- 交付使用独立新目录，不含密钥、用户数据库、私人截图或浏览器配置。Windows 传输与目标端运行状态见独立交付回执。

完整记录见 [验证报告](verification/REPORT.md)。
