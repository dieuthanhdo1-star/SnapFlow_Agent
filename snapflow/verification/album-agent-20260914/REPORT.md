# Windows 相册截图 Agent · 2026-09-14 验证

## 本次交付

- Windows 原生文件夹选择、一次授权、后台扫描、已有截图选择、暂停/继续/撤销；不会移动或删除原图。
- 任务、待办、作业、日程、灵感、资料六个处理能力；一次识别可拆出多条事项，提取课程和执行清单。
- 可靠内容自动归档；关键字段不全留待核对；可批量选择、去重、搜索、筛选、编辑、完成、查看原图。
- 飞书 OAuth、我的任务和可写日历连接，按明确的目标授权自动同步；失败不自动重试；支持日程 .ics 导出。
- 双击 Windows 启动程序自动打开页面，独立后台控制台继续运行。旧版与新版同端口时不会把旧页面当作新版启动成功。
- 首页移除旧演示、指南和无关操作；发行包不携带旧 ZIP、样例库、用户数据或真实凭据。

## 验证结果

- **68 项后端/HTTP 检查通过**：包括无授权不读取、许可令牌不可重放、已有图片范围、重复图片不重复调用、暂停撤销、处理中撤销、失败不自动重试、重启持久化、目录及软链接范围、每日新图上限、关键字段核对、结果编辑、飞书目标绑定、任务毫秒时间戳、重复同步和 CSRF。
- **9 组真实 Chrome 离线交互检查通过**：授权、批量、自动归档、筛选、编辑输入在轮询时保留、安全显示、日历导出、完成标记、保存后改分类、重复批次、同步许可、撤销、刷新持久化及 390px 布局。模型、飞书响应及原生选择器为测试替身。
- 指定云主机 **15 次真实模型网关调用**，请求 `gemini-3.8-flash`；冻结提示后单轮执行，15/15 返回有效结构、15/15 通过预先定义的事项类型、数量和所列关键字段规则；20 条事项，新增 skill 与 kind 对应有效。
- 将这些云端响应回放进最终应用：**17 条自动保存，3 条待核对**。待核对为缺具体日期的读书报告、缺结束时间的讲座、只有日期没有钟点的报名截止；未擅自补时间。
- 完整响应耗时中位数 **8.801 秒**；报告 token 总计 **40684**。人民币费用未知，模型路由别名不证明原厂身份。

## 验证边界

截图集包含 12 张虚构中文场景截图和 3 张大学官网页面截图，覆盖聊天多事项、PPT 作业、评分资料、海报、梗图、改期、不同人群、截图指令注入、未购票查询和相对日期。这不是独立真实用户盲测；规则通过不表示逐字 OCR 和每个提取字段都正确。该图片集的技能分布为作业 5、任务 6、日程 6、灵感 1、资料 2；生活待办由离线流程用例覆盖，尚未包含在本次真实图片回归中。

真实飞书账号尚未授权，因此未宣称已创建真实飞书任务/日程或验证提醒。尚未在目标 Windows 电脑执行原生选择器、启动器和后台扫描；目前通过的是开发端 Python/HTTP 和 Chrome 验证。Windows 需要已有 Python 3.10+，本次没有远程安装或覆盖 Windows 文件。

当前是单用户本机版本，不含公共托管的应用登录、计费平台、双向同步和安装包。应用提供者仍需准备模型与飞书配置，使用者随后授权。产品不会自动解答作业、执行截图命令、打开链接或给其他人发消息。

## 可复核证据

当前目录含测试日志、浏览器报告、模型摘要、规则检查和冻结提示。原始图像、响应及回放数据库保存在开发项目的 `tmp/album-agent-20260914` 和先前冻结截图集目录；未塞入发行包。云端结果保存在 `/mnt/lizekai/AgentM/evals/album-agent-20260914`，未训练、未修改既有环境；该云主机无 tmux，采用约定的 setsid + tee，复查日志显示 COMPLETE 15。

包内 MANIFEST.json 列出每个文件的大小与 SHA-256；外部 .sha256 校验整个 ZIP。发送成功仅证明传输工具接收，目标 Windows 的实际落盘目录与运行结果另行核验。

## 启动诊断修正版

新增 8 项启动检查通过，最终页面重新通过 9 组真实 Chrome 离线交互。启动器现在显示步骤、绕过代理检查回环地址、检查首页资源、记录后台错误并显示日志位置；启动窗口保留文字供用户核对。前端 API 增加超时及持续错误提示。后端及模型提示未变化，原 68 项后端与 15 张云端回归记录仍适用于对应代码。

此诊断版随后收到了目标电脑日志：实际为监听 127.0.0.1:8765 时返回 WinError 10013。Windows 限制的具体来源尚未核实。

## WinError 10013 自动换端口版

- 目标电脑 Python 3.12 日志确认，后台在绑定 127.0.0.1:8765 时因 WinError 10013 退出。
- 启动器检查可用端口；真正绑定时仍会处理拒绝/占用错误，依次尝试其他固定端口和系统分配端口。后台以当前子进程 PID 对应的启动记录报告实际监听端口，浏览器随后打开真实地址。
- 程序目录对应的成功端口会保存在本机运行目录，同一目录下次启动优先复用。飞书设置页显示 location.origin 对应的真实回调地址；开放平台白名单仍需应用所有者更新。
- **82 项后端、启动及端口集成检查通过**，包括模拟 Windows 10013、端口全部拒绝、不掩盖其他系统错误，以及真实占用端口后换端口/启动记录/浏览器地址传递。模型被禁用，测试使用隔离数据。
- **9 组 Chrome 离线交互检查通过**，额外断言非 8765 端口的飞书回调地址显示正确。模型提示未变，本次未新增付费模型调用。
- 目标 Windows 尚未执行此最终包；不能把开发端测试等同于目标机修复成功。没有要求关闭防火墙、改动系统保留端口或使用管理员权限。

参考：[微软 10013 与排除端口说明](https://learn.microsoft.com/en-us/troubleshoot/windows-server/networking/error-10013-wsaeacces-is-returned)。该文档说明一种可能原因，不证明目标 Windows 电脑的具体保留规则。

## 文件夹入口修正

用户报告选择按钮“完全没有反应”。代码检查发现，原入口会在识别服务未连接时先请求设置；全部按钮也在初始配置和状态请求成功后才注册。未取得目标电脑的浏览器错误日志，不能断言具体是哪一个原因。

现在优先注册交互，按钮立即打开页面内选择面板。原生浏览是独立按钮，并可直接填写绝对路径；后台仍先验证路径、签发一次性令牌，再在用户明确确认后授权。未配置模型时只保留授权，图片识别等待连接后启动。

84 项 Python/HTTP/启动检查通过；11 组 Chrome 离线交互通过。新增的故障注入主动阻止首次 /api/config 请求，确认错误可见且文件夹面板仍可打开；该预期网络失败已单独记录，其他浏览器错误为空。新增用例覆盖未配置模型时保存文件夹、没有图片被提前标记为失败、配置启用后继续识别，以及相对路径/不存在路径/普通文件被拒绝。原有自动换端口测试保留。

本次未运行模型回归（识别提示未改），没有付费模型调用；真实 Windows 原生浏览器和飞书仍需目标机验收。

## ConfigFix：Windows 配置继承修复

2026-09-14：95 项后端、HTTP、启动及配置继承回归全部通过；11 组真实 Chrome 离线交互重新通过。新增的 11 项配置用例覆盖旧版私有配置继承、多层解压、旧版移除后复用私有副本、当前配置优先、冲突账号不擅自选择、损坏配置不使启动崩溃、缺失配置提示、禁用开关、测试隔离和保存持久化。

默认网关模型保持 gemini-3.8-flash。成功读取的本机配置保存在项目 .snapflow-config 中，发行包不含真实密钥。启动器使用新的版本标识，避免复用尚在运行的旧后台。真实 Windows 文件夹位置与密钥位置尚未取得；远端 SSH 拒绝连接，因此不宣称已修复目标电脑的实际运行状态。本次未执行任何模型调用，模型提示及技能处理代码未改动，原模型记录为历史依据。

证据：configfix-checks.log、configfix-browser.json、RELEASE_VALIDATION.json。

## 页面内文件夹选择与持续监测更新

102 项后端/HTTP/启动回归通过，12 组 Chrome 离线交互通过。点击授权后直接在网页内浏览磁盘、文件夹、返回上一级，无需等待外部 PowerShell 选择窗口。目录浏览只读取目录名称，不读取图片，也不会自动授权；在选定目录后点击“授权并开始”才保存许可并允许后续识别。Linux 开发端浏览范围限制在项目内。

后台每 3 秒检查一次，连续观察到稳定文件且写入至少经过 2 秒后再处理；新增和同名修改截图自动入队，相同内容去重。扫描历史一次批量读取，避免逐图片反复打开数据库。新增实际运行监测线程的离线用例验证新增、修改、暂停、继续、撤销与未完成写入；模型调用为替身，本次没有真实模型请求。

页面内目录导航和桌面/390px 窄屏布局已检查。Windows 更新入口只更新指定安装目录内的程序文件，核验备份后重启匹配完整 server.py 路径的 Python 后台，保留 data 和 .snapflow-config。离线校验了备份、文件摘要、用户数据保留和进程匹配表达式；尚未在目标 Windows 上实际执行驱动器枚举、进程重启及端到端识别。
